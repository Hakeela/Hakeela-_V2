function Blog() {
    return (
        <main className="page-shell">
            <div className="container">
                <section className="page-shell__card">
                    <p className="page-shell__eyebrow">Insights</p>
                    <h1>HakVersity Blog</h1>
                    <p>
                        Stories, lessons, updates, and opportunities from the HakVersity learning community.
                    </p>
                    <p>
                        This page is now set up as an internal route so the site navigation works correctly in the
                        app without relying on placeholder links.
                    </p>
                </section>
            </div>
        </main>
    )
}

export default Blog
